# agent-trace

Extract agent session trace libraries from mindwalk — classify tool actions, tail live session logs, and emit OpenTelemetry spans. Used by Harness for idle detection and trajectory export to Cairn.